<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@color/window_bg</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomCirclePageIndicatorSuggestedServices">
        <item name="fillColor">@android:color/white</item>
        <item name="strokeColor">@android:color/white</item>
        <item name="radius">@dimen/_4sdp</item>
        <item name="strokeWidth">@dimen/fix_1dp</item>
        <item name="pageColor">@android:color/transparent</item>
    </style>

    <style name="CustomCirclePageIndicator">
        <item name="fillColor">@color/colorAccent</item>
        <item name="strokeColor">@color/intro_pager_stroke_color</item>
        <item name="radius">@dimen/_4sdp</item>
        <item name="strokeWidth">@dimen/fix_1dp</item>
        <item name="pageColor">@android:color/white</item>
    </style>

    <style name="customTabFontsStyle" parent="TextAppearance.Design.Tab">

        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/text_size_small</item>
        <item name="textAllCaps">false</item>
    </style>

</resources>
